

<br>
<% user = User.find(@photo.user_id) %>
<section class="hero">
       <div class="container">
        <div class="row">

     <div class="col-lg-6 offset-lg-3">

    <div class="cardbox shadow-lg bg-white">

     <div class="cardbox-heading">
			 <h4 class="text-center">
		 		<%= @photo.name %>
		 	</h4>


      <div class="media m-0">
       <div class="d-flex mr-3">
      <a href=""><%= image_tag user.image_url, alt:  user.first_name, class:"img-fluid rounded-circle" %>
        </a>
       </div>
       <div class="media-body">
        <p class="m-0"> <%= link_to "#{user.first_name} #{user.last_name}", user_path(user),class: '' %></p>

      <small><span><i class="icon ion-md-time"></i> <%= @photo.updated_at%></span></small>
       </div>
      </div><!--/ media -->
     </div><!--/ cardbox-heading -->

     <div class="text-center  ">
        <a data-toggle="modal" data-target="#myModal<%=@photo.id%>"><%= image_tag @photo.photography.show.url %></a>

     </div><!--/ cardbox-item -->
     <div class="cardbox-base text-muted votes-count-<%=@photo.id%>"  >
  <%= render '/likes/likes',  photo: @photo %>
<div>
  <div id="commentform" class="commentform-<%=@photo.id%>">
  <% if @photo.comments.any? %>
  <p align='center'>Last comment:</p>
  <% @comments = @photo.comments.order(updated_at: :desc, id: :asc).where(parent_comment_id: nil) %>
  <% @printed_comment = [] %>

  <%= render '/comments/comments', comments: @comments , photo: @photo %>
   </div>
   <%= link_to 'Share', @photo.share(request.original_url) , target: :_blank %>

        <% else %>

          <p align='center'>No comments here...</p>
        <% end %>
        </div>


<% if current_user %>

       <%= form_for [@photo, @photo.comments.build], remote: true do |f| %>
     <div class="cardbox-comments">
      <span class=" comment-avatar float-left">
       <a href=""><%= image_tag current_user.image_url, alt:  current_user.first_name, class:"rounded-circle" %></a>
      </span>
      <div class=" container search">
       <%= f.text_area :body, class: 'commentinp' ,placeholder:"Write a comment",size: "35x2" ,required: true%>
       <%= f.submit 'Post', class: '  btn-sm btn-primary text-right' %>
       <% end %>
     <% else %>
         <p>you need sign in</p>
     <% end %>
</div>
</div>

      </div><!--/. Search -->
     </div><!--/ cardbox-like -->

    </div><!--/ cardbox -->

         </div><!--/ col-lg-6 -->


				 <div class="modal fade" id="myModal<%=@photo.id%>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
				 		<div class="modal-dialog modal-lg">
				 			<div class="modal-content">
				 				<div class="modal-header">
				 					<h6 class="text-white ">
				 						<%= @photo.name %></h6>
				 					<button type="button" class="close btn" data-dismiss="modal"><span aria-hidden="true" class="text-danger">×</span></button>
				 				</div>
				 				<div class="modal-body ">

				 					<%= image_tag @photo.photography.url%>

				 				</div>
				 			</div>
				 		</div>
				 	</div>
