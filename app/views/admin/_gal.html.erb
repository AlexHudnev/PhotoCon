
<div>
<%= paginate photos ,
                   :pagination_class => " pagination justify-content-center "%></div>
                   <br><br>
<div clss=" row "
<% photos.each do |photo| %>




   <% user = User.find(photo.user_id) %>



    <%= link_to (image_tag photo.photography.rating.url), admin_photo_path(photo)%>


<%end%>

</div>
