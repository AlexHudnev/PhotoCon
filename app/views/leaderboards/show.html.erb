<h1>leaderboard.subtitle %></h1>

<table style='width: 100%'>
  <tr>
    <th>entries.rank</th>
    <th>entries.name</th>
    <th>entries.score></th>
    <th>labels.delete</th>
  </tr>
  <% @entries.each do |entry|  %>
    <%= render_cell :entry, :show, entry: entry %>
  <% end %>
</table>
<span style='font-size: small; font-style:italic; float:right'><%= t('labels.edit_help') %></span>
<%= paginate @page_array %>
